services:
  app:
    build: .
    ports:
      - "8081:8081"
    volumes:
      # Live-reload: mount source but keep node_modules from the image
      - .:/app
      - /app/node_modules
    env_file:
      - path: .env
        required: false
    environment:
      - EXPO_NO_TELEMETRY=1
